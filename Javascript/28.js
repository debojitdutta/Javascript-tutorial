// strict mode


// You can put it:

// At the top of a .js file — applies to the whole file.

// Inside a function — applies only to that function.

// Example inside a function:

function test() {
  "use strict";
  y = 10; // Error
}
test();